services:
  ssh-box:
    build: .
    image: ssh-box:latest
    ports:
      - "${SSH_PORT:-2222}:22"
    volumes:
      - "${SSH_PUBLIC_KEY_PATH:-${HOME}/.ssh/id_ed25519.pub}:/etc/ssh/authorized_keys/aoliyougei:ro"
    restart: unless-stopped
